export const counterReducer = (state: any, action: any): any => {
    switch (action.type){
        case 'INCREMENT':{
            return state
        }
        case 'INCREMENT':{
            return state
        }
        case 'INCREMENT':{
            return state
        }
        case 'INCREMENT':{
            return state
        }
        default:{
            return state
        }
    }
}